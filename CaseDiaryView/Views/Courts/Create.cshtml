@model CaseDiaryView.ViewModels.Court

@{
    ViewData["Title"] = "Create Court";
}

<h2>Create Court</h2>

<form asp-action="Create" method="post">
    <div class="form-group">
        <label asp-for="CourtName" class="control-label">Name</label>
        <input asp-for="CourtName" class="form-control" required />
        <span asp-validation-for="CourtName" class="text-danger"></span>
    </div>

   @*  <div class="form-group">
        <label asp-for="Cases" class="control-label">case</label>
        <textarea asp-for="Cases" class="form-control" rows="3"></textarea>
        <span asp-validation-for="Cases" class="text-danger"></span>
    </div> *@

    <br />
    <button type="submit" class="btn btn-success">Create</button>
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
